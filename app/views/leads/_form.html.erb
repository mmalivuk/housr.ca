<div class="leadform">
<%= form_for(@lead) do |f| %>
  <% if @lead.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lead.errors.count, "error") %> prohibited this lead from being saved:</h2>

      <ul>
      <% @lead.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :first_name %>&nbsp
    <%= f.text_field :first_name %>
  </p>
  <p>
    <%= f.label :last_name %>&nbsp
    <%= f.text_field :last_name %>
  </p>
  <p>
    <%= f.label :status %>&nbsp
	<%= f.select :status, [['Lead','Lead'],['Client','Client'],['Previous Client','Previous Client']] %>
  </p>
  <p>
    <%= f.label :phone %>&nbsp
    <%= f.text_field :phone %>
  </p>
  <p>
    <%= f.label :email %>&nbsp
    <%= f.text_field :email %>
  </p>
  <p>
    <%= f.label :looking_to %>&nbsp
	<%= f.select :looking_to, [['Buy','Buy'],['Sell','Sell'],['Rent','Rent'],['Other','Other']] %>
  </p>
  <p>
    <%= f.label :notes %>&nbsp
    <%= f.text_area :notes, :class => "xxlarge", :rows => "7" %>
  </p>
    <%= f.hidden_field :user_id, :value => current_user.id %>
<br>
  
    <%= f.submit "Add Client", :class => "leadsubmit" %>
<% end %>
<br class="clearfloat"><br>
</div>
